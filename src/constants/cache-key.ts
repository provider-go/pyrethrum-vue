/** 缓存数据时用到的 Key */
class CacheKey {
  static readonly TOKEN = `token-key`
  static readonly CONFIG_LAYOUT = `config-layout-key`
  static readonly SIDEBAR_STATUS = `sidebar-status-key`
  static readonly ACTIVE_THEME_NAME = `active-theme-name-key`
  static readonly VISITED_VIEWS = `visited-views-key`
  static readonly CACHED_VIEWS = `cached-views-key`
}

export default CacheKey
